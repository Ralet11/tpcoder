block content
  style.
    body {
      background: linear-gradient(45deg, #171738, #1A2342, #1B2A4F);
    }

    .nav {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 10px;
      background-color: #171738;
    }

    .cart-button {
      background-color: #4caf50;
      color: white;
      padding: 10px 20px 10px 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .cart-button:hover {
      background-color: #45a049;
    }

    .chat-button {
      background-color: #109DFA;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-left: 10px;
      margin-right: 10px;
    }

    .chat-button:hover {
      background-color: #109DDA;
    }

    .logo {
      width: 50px;
      height: 50px;
      margin-right: auto;
    }

    .logo-container {
      display: flex;
      align-items: center;
      margin-right: auto;
    }

    .logo-text {
      margin-left: 10px;
      color: white;
      font-size: 16px;
    }

    h1 {
      color: white;
      font-size: 24px;
      margin-bottom: 20px;
    }
    .chat-container {
      width: 400px;
      height: 500px;
      background-color: #f1f1f1;
      border: 1px solid #ccc;
      border-radius: 5px;
      overflow-y: auto;
      padding: 10px;
      margin-bottom: 20px;
      margin-right: auto;
      margin-left: auto;
    }

    .chat-message {
      background-color: #fff;
      border-radius: 5px;
      padding: 10px;
      margin-bottom: 10px;
    }

    .chat-message-sender {
      margin: 0;
      font-weight: bold;
    }

    .chat-message-content {
      margin: 0;
    }

  div.nav
    div.logo-container
      img.logo(src="/imagenes/logo.png", alt="Logo de la empresa", class="logo")
      span.logo-text Action Games Improved
    button.cart-button(onclick="outCart()") Volver a la tienda
    button.chat-button(onclick="openChat()") Chatea con nosotros

  h1 HIstorial de chat Personales

  .chat-container
    .chat-messages
      each chat in chats
        .chat-message
          p.chat-message-sender #{chat.name}
          p.chat-message-content #{chat.msg}
  script.

        function openChat() {
          window.location.href = "/api/chat";
        }

        function outCart() {
          window.location.href = "/api/products";
        }

